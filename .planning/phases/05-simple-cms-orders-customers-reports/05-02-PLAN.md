---
phase: 05-simple-cms-orders-customers-reports
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - app/lib/checkout.ts
  - app/components/cart/CartSidebar.tsx
  - app/cms/page.tsx
  - app/cms/orders/page.tsx
  - app/cms/reports/page.tsx
autonomous: false
requirements:
  - MGMT-01
  - MGMT-02
must_haves:
  truths:
    - "Order is saved to Supabase BEFORE WhatsApp link opens"
    - "Order ID is included in WhatsApp message for reference"
    - "Admin can view orders in CMS"
    - "Admin can view sales and product reports"
  artifacts:
    - path: "app/lib/checkout.ts"
      provides: "Modified checkout to save order to Supabase first"
    - path: "app/cms/page.tsx"
      provides: "CMS dashboard with key metrics"
    - path: "app/cms/orders/page.tsx"
      provides: "Order list with filtering"
    - path: "app/cms/reports/page.tsx"
      provides: "Sales reports with charts per D-14 to D-17"
  key_links:
    - from: "app/components/cart/CartSidebar.tsx"
      to: "app/lib/supabase.ts"
      via: "saveOrderToSupabase function"
    - from: "app/cms/reports/page.tsx"
      to: "supabase"
      via: "SQL queries for metrics"
---

<objective>
Modify checkout flow to save orders to Supabase before WhatsApp, and create admin CMS with orders list and sales reports.

Purpose: Enable order persistence and provide admin dashboard for managing orders and viewing metrics.
Output: Modified checkout, CMS dashboard, orders page, reports page with charts.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-simple-cms-orders-customers-reports/05-CONTEXT.md
@.planning/phases/05-simple-cms-orders-customers-reports/05-01-PLAN.md
@.agents/skills/frontend-design/SKILL.md

# Key interfaces
From app/lib/store.ts:
```typescript
export interface CartItem {
  id: string
  name: string
  price: number
  quantity: number
  image?: string
}
```

From app/lib/checkout.ts:
```typescript
export function createWhatsAppLink(message: string): string
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create order saving function in Supabase client</name>
  <files>app/lib/supabase.ts</files>
  <read_first>
    - app/lib/supabase.ts
    - supabase/schema.sql
  </read_first>
  <behavior>
    - Test 1: saveOrder returns order_id on success
    - Test 2: saveOrder includes all order items
    - Test 3: saveOrder handles customer upsert
    - Test 4: Returns error on DB failure
  </behavior>
  <action>
Add to app/lib/supabase.ts:
1. saveOrder function:
   - Accept: customer data (name, phone, address), items[], total
   - First upsert customer (find by phone or create new)
   - Generate order_id: ORD-YYYYMMDD-XXXX (per D-19)
   - Insert order record
   - Insert all order_items with subtotal
   - Return order_id on success
   
2. getOrders function for CMS:
   - Fetch orders with order_items
   - Support filters: status, date range
   
3. getReports function:
   - Total revenue (sum of order totals)
   - Orders count
   - Average order value (total / count)
   - Top selling products (group by product_name, sum quantity)
   - Units sold per product
  </action>
  <verify>
    <automated>grep -q "saveOrder" app/lib/supabase.ts && grep -q "getOrders" app/lib/supabase.ts</automated>
  </verify>
  <done>Supabase order operations added to client</done>
</task>

<task type="auto">
  <name>Task 2: Modify checkout to save to Supabase before WhatsApp</name>
  <files>app/lib/checkout.ts</files>
  <read_first>
    - app/lib/checkout.ts
    - app/lib/supabase.ts (created in plan 1)
    - app/lib/store.ts (for CartItem interface)
  </read_first>
  <action>
Modify app/lib/checkout.ts:
1. Add import for supabase client
2. Create async saveOrderAndGenerateLink function:
   - Call saveOrder from supabase client
   - Get returned order_id
   - Modify generateOrderMessage to include order_id
   - Then call createWhatsAppLink
   - Return { orderId, whatsappLink }
   
3. Per D-01 to D-04: Flow must be Form → Save to Supabase → Get order ID → Open WhatsApp
4. Per D-20: Show optimistic UI - display loading while saving to DB
5. Per D-03: Include order ID in WhatsApp message: "Número de pedido: ORD-20260427-1234"
  </action>
  <verify>
    <automated>grep -q "saveOrder" app/lib/checkout.ts && grep -q "order_id" app/lib/checkout.ts</automated>
  </verify>
  <done>Checkout flow saves to Supabase before opening WhatsApp per D-01 to D-04</done>
</task>

<task type="auto">
  <name>Task 3: Update CartSidebar checkout button to use new flow</name>
  <files>app/components/cart/CartSidebar.tsx</files>
  <read_first>
    - app/components/cart/CartSidebar.tsx
    - app/lib/checkout.ts (modified in Task 2)
  </read_first>
  <action>
Modify CartSidebar:
1. Import new saveOrderAndGenerateLink function
2. Change checkout button onClick:
   - Set loading state (per D-20 optimistic UI)
   - Await saveOrderAndGenerateLink()
   - Open WhatsApp link in new tab
   - Clear cart (per WATS-05)
   - Handle errors gracefully
   
3. Show loading spinner/text while saving to Supabase
4. Ensure button is disabled during save to prevent double-submit
  </action>
  <verify>
    <automated>grep -q "saveOrderAndGenerateLink" app/components/cart/CartSidebar.tsx</automated>
  </verify>
  <done>Checkout button now saves order to Supabase before WhatsApp</done>
</task>

<task type="auto">
  <name>Task 4: Create CMS dashboard page</name>
  <files>app/cms/page.tsx</files>
  <read_first>
    - app/cms/layout.tsx
    - app/lib/supabase.ts
  </read_first>
  <action>
Create app/cms/page.tsx (Server Component):
1. Fetch key metrics using getReports():
   - Today's orders count
   - This week's revenue
   - Pending orders
   - Repeat customer rate (per D-17)
   
2. Display cards with:
   - Total revenue
   - Orders today
   - Pending orders
   - Average order value
   
3. Quick links to: Ver pedidos, Ver reportes

4. Use design tokens from project - clean admin aesthetic
  </action>
  <verify>
    <automated>test -f app/cms/page.tsx && grep -q "getReports" app/cms/page.tsx</automated>
  </done>
  <done>CMS dashboard displays key metrics</done>
</task>

<task type="auto">
  <name>Task 5: Create orders list page</name>
  <files>app/cms/orders/page.tsx</files>
  <read_first>
    - app/cms/page.tsx (for patterns)
    - app/lib/supabase.ts
  </read_first>
  <action>
Create app/cms/orders/page.tsx:
1. 'use client' for filtering/interactivity
2. Fetch orders using getOrders()
3. Display table with columns:
   - Order ID (ORD-YYYYMMDD-XXXX)
   - Customer name
   - Total
   - Status (pending/confirmed/delivered)
   - Date
4. Add filters:
   - Status dropdown
   - Date range picker (today/week/month per D-16)
5. Each row clickable for order detail (expand order_items)
6. Status update buttons: Mark as confirmed, Mark as delivered
  </action>
  <verify>
    <automated>test -f app/cms/orders/page.tsx && grep -q "getOrders" app/cms/orders/page.tsx</automated>
  </done>
  <done>Orders list page with filtering per D-10 to D-13</done>
</task>

<task type="auto">
  <name>Task 6: Create reports page with charts</name>
  <files>app/cms/reports/page.tsx</files>
  <read_first>
    - app/cms/orders/page.tsx (for patterns)
    - app/lib/supabase.ts
  </read_first>
  <action>
Create app/cms/reports/page.tsx:
1. 'use client' for interactive charts
2. Time filter: Today / This Week / This Month / Custom range (per D-16)
3. Sales metrics (per D-14):
   - Total revenue (sum of order totals)
   - Orders count
   - Average order value
4. Product metrics (per D-15):
   - Top selling products (bar chart)
   - Units sold per product
5. Use chart library: recharts (per D-22 agent discretion)
6. Display in responsive grid layout (per D-21 agent discretion)
  </action>
  <verify>
    <automated>test -f app/cms/reports/page.tsx && grep -q "recharts" app/cms/reports/page.tsx</automated>
  </done>
  <done>Reports page with sales and product metrics per D-14 to D-17</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify checkout and CMS</name>
  <what-built>Checkout integration and admin CMS with reports</what-built>
  <how-to-verify>
    1. Complete checkout flow - verify order appears in /cms/orders
    2. Check WhatsApp message includes order ID
    3. Visit /cms and verify dashboard loads with metrics
    4. Visit /cms/orders and verify order list displays
    5. Visit /cms/reports and verify charts render with data
    6. Test time filter changes on reports page
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <action>Human verification required - see how-to-verify for steps</action>
  <done>Verified by human: type "approved" when complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Checkout → Supabase | Cart data submitted to database |
| CMS → Supabase | Admin reads all order data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | Order data | mitigate | Validate order total matches items sum server-side |
| T-05-06 | Repudiation | Order creation | mitigate | Store created_at timestamps, unique order IDs |
| T-05-07 | Information Disclosure | Reports | mitigate | CMS behind auth, RLS ensures only admin sees data |
| T-05-08 | Denial of Service | Supabase free tier | accept | Rate limiting built into Supabase free tier |
</threat_model>

<verification>
- [ ] Order saves to Supabase before WhatsApp opens
- [ ] Order ID appears in WhatsApp message
- [ ] Cart clears after checkout
- [ ] CMS dashboard shows key metrics
- [ ] Orders list displays with filtering
- [ ] Reports page shows charts with correct data
</verification>

<success_criteria>
- Customer can complete checkout and order is persisted in Supabase
- Admin can view and manage orders via CMS
- Sales reports display accurate metrics
- Time-based filtering works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/05-simple-cms-orders-customers-reports/05-02-SUMMARY.md`
</output>